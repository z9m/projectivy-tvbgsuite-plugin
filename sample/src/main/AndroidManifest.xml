<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.butch708.projectivy.tvbgsuite"
    tools:ignore="MissingLeanbackLauncher">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />

    <queries>
        <package android:name="com.spocky.projengmenu" />
        <package android:name="org.moonfin.androidtv" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/plugin_name"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher"
        android:banner="@mipmap/ic_banner"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.ProjectivyWallpaperProvider">

        <!-- Settings Activity: Exported and with launcher intent for testing -->
        <activity
            android:name="tv.projectivy.plugin.wallpaperprovider.sample.SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="tv.projectivy.plugin.wallpaperprovider.sample.WallpaperProviderService"
            android:exported="true"
            android:label="@string/plugin_name"
            android:icon="@mipmap/ic_launcher"
            android:banner="@mipmap/ic_banner"
            tools:ignore="ExportedService">

            <!-- Mandatory for the plugin to be detected by Projectivy -->
            <intent-filter>
                <action android:name="tv.projectivy.plugin.WALLPAPER_PROVIDER"/>
            </intent-filter>

            <!-- Api version this plugin is compatible with -->
            <meta-data
                android:name="apiVersion"
                android:value="1"/>

            <!--Plugin UUID -->
            <meta-data
                android:name="uuid"
                android:value="@string/plugin_uuid"/>

            <!-- Plugin name -->
            <meta-data
                android:name="name"
                android:value="@string/plugin_name"/>

            <!-- Plugin activity called by Projectivy to configure it -->
            <meta-data
                android:name="settingsActivity"
                android:value="tv.projectivy.plugin.wallpaperprovider.sample.SettingsActivity"/>

            <!-- How long should Projectivy keep wallpapers before a new request -->
            <meta-data
                android:name="itemsCacheDurationMillis"
                android:value="@integer/items_cache_duration_millis"/>

            <!-- Update mode -->
            <meta-data
                android:name="updateMode"
                android:value="17"/>
        </service>

    </application>

</manifest>